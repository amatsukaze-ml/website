// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id   Int    @id @default(autoincrement())
  name String
  mods Mod[]  @relation(name: "ModToUser")
}

model Mod {
  id          Int      @id @default(autoincrement())
  name        String
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  Type        ModType
  Platforms   Bytes    @db.VarBinary(3)
  creator_id  Int
  creator     User     @relation(name: "ModToUser", fields: [creator_id], references: [id])
}

enum ModType {
  CORE
  GAMEPLAY
  COSMETICS
  EDITOR
  OTHER
}
